---
artifact: curriculum-tutorial
leaf: training/single-gpu-loop/training-hyperparameters
priority: P0
environment: Mac
---

# 导学：超参不是菜单，它们分别改变统计、更新和资源边界

本叶先建立 batch size、sequence length、learning rate、weight decay、steps/epochs、gradient accumulation、warmup 的因果图。目标不是背默认值，而是知道改一个参数会碰到训练循环的哪一段。
